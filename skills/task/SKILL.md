# 任务追踪领域知识

## task_tracker
会话内任务清单追踪工具。用于将复杂任务拆解为步骤并逐步推进。

## 使用流程
1. 首次调用传入 `tasks` 列表建立任务清单，返回第一步
2. 后续调用 `next_step=true` 逐步推进
3. 所有步骤完成后自动清空

## 常见陷阱
- 状态仅在当前会话内有效，重启丢失
- tasks 参数和 next_step 参数互斥，不要同时传
- 任务完成后再次调用 next_step 会返回错误，需重新建立清单
